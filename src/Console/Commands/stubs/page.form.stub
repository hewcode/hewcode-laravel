<?php

namespace {{ namespace }};

use Hewcode\Hewcode\Actions;
use Hewcode\Hewcode\Panel\Controllers\Resources\FormController;
use Hewcode\Hewcode\Props;

class {{ class }} extends FormController
{
    public static string $form = {{ form }}::class;

    protected string $view = '{{ view }}';

    protected string $icon = '{{ icon }}';

    {{ navigationProperty }}

    public function panels(): array
    {
        {{ panels }}
    }

    protected function props(Props\Props $props): Props\Props
    {
        return $props->appendData([
            // Additional data for this page
        ]);
    }

    protected function getHeaderActions(): array
    {
        return [
            Actions\Eloquent\SaveAction::make(),
            Actions\Eloquent\CancelAction::make(),
        ];
    }
}
